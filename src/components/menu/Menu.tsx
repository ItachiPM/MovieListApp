import React from "react";
import {Title} from "../title/Title";
import {MovieType} from "../movieType/MovieType";

export const Menu = () => {
    return <>
        <Title/>
        <MovieType/>
    </>
}
